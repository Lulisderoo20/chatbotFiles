# Django Chatbot Experiment

Setup and run:

```powershell
cd experiments/django_chat
python manage.py migrate
python manage.py runserver
```

Endpoints:

- POST `/chatbot/chat/`
- GET `/chatbot/history/`
